import 'package:flutter/material.dart';

class AddName{
  String name;
  Color textColor;
  double  textSize;
  String fontFamily;
  bool isSelected=false;

  AddName(this.name, this.textColor, this.textSize, this.fontFamily);
}